{
  "name": "9-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker:run": "docker run --env NAME=Oleg --env SURNAME=Korn --rm --name mynode -it mynode",
    "docker:first_node": "docker run -d -v /data:/var/first/data --name first_node -it node",
    "docker:second_node": "docker run -d -v /data:/var/second/data --name second_node -it node"
  },
  "author": "",
  "license": "ISC"
}
